version: 0.0.3
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  alicloud-oss: 0.0.3
icon: ./icon.png
name: pdf_craft_cloud
displayName: "%display-name%"
description: "%description%"
repository: https://github.com/oomol-flows/pdf-craft-cloud.git
releaseNotes: |
  - Added error handling options for PDF parsing and OCR failures
  - Enhanced PDF conversion workflow with progress monitoring
  - Improved localization support for UI descriptions
  - Updated documentation for better clarity and user focus
